[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing (e.g. /employees/123/edit) —
# without this, refreshing on any page other than "/" would 404 on Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
